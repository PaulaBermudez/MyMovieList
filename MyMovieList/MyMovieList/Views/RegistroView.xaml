<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MyMovieList.ViewModels"
             x:Class="MyMovieList.Views.RegistroView">
    <ContentPage.BindingContext>
        <viewmodels:UsuarioViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout VerticalOptions="CenterAndExpand" HorizontalOptions="FillAndExpand">
            <Label Text="Usuario" HorizontalOptions="CenterAndExpand"/>
            <Entry Text="{Binding Usuario.NombreUsuario}" HorizontalOptions="FillAndExpand"/>
            <Label Text="Email" HorizontalOptions="CenterAndExpand"/>
            <Entry Text="{Binding Usuario.Email}" HorizontalOptions="FillAndExpand"/>
            <Label Text="Contraseña" HorizontalOptions="CenterAndExpand"/>
            <Entry Text="{Binding Usuario.Password}" HorizontalOptions="FillAndExpand"/>
            <Label Text="Confirmar contraseña" HorizontalOptions="CenterAndExpand"/>
            <Entry Text="{Binding Usuario.Password2}" HorizontalOptions="FillAndExpand"/>
            <Button Command="{Binding NuevoUsuario}" Text="Crear Usuario" HorizontalOptions="CenterAndExpand"/>
            <Label Text="{Binding Error}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>